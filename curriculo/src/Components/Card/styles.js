 import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  container:{
    flex: 1,
    width: 300,
    backgroundColor: '#fff',
    borderRadius: 50,
    padding: 10,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 40
  },

  title:{
    fontSize: 20,
    textAlign: 'center',
    fontWeight: 'bold'
  },

  text:{
    fontSize: 14,
    padding: 16
  }
})